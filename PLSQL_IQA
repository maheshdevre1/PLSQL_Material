1. What are the features of PL/SQL?
2. What do you understand by PL/SQL Table?
3. Explain the basic structure followed in PL/SQL.
4. What is a PL/SQL cursor?
5. What is the use of WHERE CURRENT OF in cursors?
6. How can a name be assigned to an unnamed PL/SQL Exception Block?
7. What is a PL/SQL Trigger? Give some examples of when "Triggers" might be useful.
8. When does a DECLARE block have to be present?
9. How should comments be written in PL/SQL code?
10. What is the purpose of the WHEN condition in the trigger?
11. What are the Differences between SQL and PL/SQL?
12. Why are SYSDATE and USER keywords used?
13. What is the Difference between implicit cursor and explicit cursor?
14. Tell the importance of %TYPE and %ROWTYPE data types in PL/SQL.
15. What are the differences between ROLLBACK and ROLLBACK TO statements in PL/SQL?
16. What are the uses of SYS.ALL_DEPENDENCIES?
17. What virtual tables exist during execution of a trigger?
19. What is purpose of COMMIT, ROLLBACK and SAVEPOINT?
20. How can we debug our PL/SQL code?
21. What is the main difference between a mutating table and a constraining table?
22. Describe the data types present in PL/SQL.
23. List the types of exceptions in PL/SQL.
24. What types of commands PL/SQL does not support?
25. Name some PL/SQL exceptions.
26. What is a PL/SQL package?
27. Write a PL/SQL program to find a given string is palindrome.
28. What command will you use to delete a package?
29. How will you execute a stored procedure?
30. Explain the IN, OUT and IN OUT parameters.
31. Differentiate between %ROWTYPE and %TYPE.
32. Discuss SQLERRM and SQLCODE. What is the importance of PL/SQL?
33. What are PL/SQL records? Tell types of records.
34. Explain the BTITLE and TTITLE statements.
35. What are the valid DateTime values for seconds in PL/SQL?
36. Explain PL/SQL Delimiters.
37. What is the use of a UTL_FILE package in PL/SQL?
38. What is the use of index in PL/SQL?
39. What does the error ORA-03113 mean?
40. What is a Join?
41. How can we write or create multiple tables in PL/SQL?
42. Discuss the concept of RAISE_APPLICATION_ERROR.
43. What do you know about PRAGMA EXCEPTION_INIT in PL/SQL?
44. How can you verify whether an UPDATE statement is executed or not in PL/SQL?
45. What is the use of the || operator?
46. Is a definition command like CREATE supported in PL/SQL?
47. Explain a view.
48. What are the basic parts of triggers?
49. What are the methods to trace PL/SQL code?
50. How do you create nested tables in PL/SQL?

1. What are the features of PL/SQL?
-----------------------------------
PL/SQL (Procedural Language/SQL) is an extension of SQL used in Oracle databases. It allows developers to write programs that 
combine SQL queries with programming features like conditions, loops, and error handling.
Features of PL/SQL:
1. Procedural Language Support
PL/SQL supports programming features like decision making (IF statements) and loops (FOR, WHILE). This makes it similar to other
programming languages.
Example:
DECLARE
   num NUMBER := 10;
BEGIN
   IF num > 5 THEN
      DBMS_OUTPUT.PUT_LINE('Number is greater than 5');
   END IF;
END;
/
2. Executes Multiple SQL Queries in One Block
PL/SQL allows multiple SQL statements to be written and executed together in a single block.
Example:
BEGIN
   INSERT INTO employees(id, name) VALUES (101, 'John');
   UPDATE employees SET name = 'Johnny' WHERE id = 101;
END;
/
3. Exception Handling (Error Handling)
PL/SQL can handle errors using an EXCEPTION block, so the program does not stop unexpectedly.
Example:
DECLARE
   num NUMBER;
BEGIN
   num := 10/0;
EXCEPTION
   WHEN ZERO_DIVIDE THEN
      DBMS_OUTPUT.PUT_LINE('Cannot divide by zero');
END;
/
4. Reusable Program Units
PL/SQL allows creating reusable database objects such as:
Procedures
Functions
Packages
Triggers
Types
These are stored in the database and can be used by many applications.
Example (Procedure):
CREATE OR REPLACE PROCEDURE greet_user
AS
BEGIN
   DBMS_OUTPUT.PUT_LINE('Hello User');
END;
/
5. Portability
Applications written in PL/SQL can run on any system where the Oracle database is installed, so they are 
portable across different hardware and operating systems.
2. What do you understand by PL/SQL Table?
-------------------------------------------
A PL/SQL Table (also called an Index-by Table or Associative Array) is a collection of elements of the same data type.
Each element is stored with an index (position number), which is used to access the value.
It works similar to an array in programming languages, where values are stored and accessed using an index.
Example:
DECLARE
    TYPE Vehicle_SSN_tabtype IS TABLE OF INTEGER INDEX BY BINARY_INTEGER;
    Vehicle_SSN_table Vehicle_SSN_tabtype;
BEGIN
    Vehicle_SSN_table(1) := 101;
    Vehicle_SSN_table(2) := 102;
    DBMS_OUTPUT.PUT_LINE(Vehicle_SSN_table(1));
END;
/
Explanation of the Example:
TYPE Vehicle_SSN_tabtype IS TABLE OF INTEGER INDEX BY BINARY_INTEGER;
This line creates a user-defined table type that stores integer values and uses numbers as indexes.
Vehicle_SSN_table Vehicle_SSN_tabtype;
This declares a PL/SQL table variable based on the type created.
Vehicle_SSN_table(1) := 101;
Stores value 101 at index 1.
Vehicle_SSN_table(2) := 102;
Stores value 102 at index 2.
DBMS_OUTPUT.PUT_LINE(Vehicle_SSN_table(1));
Prints the value stored at index 1.
3. Explain the basic structure followed in PL/SQL.
-----------------------------------------------------
In PL/SQL, the basic unit of a program is called a Block.
Every PL/SQL program is written inside a block structure.
A PL/SQL block has three main parts:
1. Declaration Section
This section is used to declare variables, constants, cursors, or types that will be used in the program.
This part starts with the keyword DECLARE.
2. Execution Section
This section contains the actual logic of the program such as SQL statements, calculations, loops, or conditions.
This part starts with the keyword BEGIN and ends with END.
It is the mandatory part of a PL/SQL block.
3. Exception Handling Section
This section handles errors that occur during program execution.
It starts with the keyword EXCEPTION.
Basic Structure Syntax:
DECLARE
   -- variable declaration
BEGIN
   -- executable statements
EXCEPTION
   -- error handling statements
END;
/
Example:
DECLARE
   num1 NUMBER := 10;
   num2 NUMBER := 0;
   result NUMBER;
BEGIN
   result := num1 / num2;
   DBMS_OUTPUT.PUT_LINE('Result: ' || result);
EXCEPTION
   WHEN ZERO_DIVIDE THEN
      DBMS_OUTPUT.PUT_LINE('Error: Division by zero');
END;
/
Explanation:
DECLARE → Variables num1, num2, and result are declared.
BEGIN → Program logic is executed (num1 / num2).
EXCEPTION → If division by zero occurs, it prints an error message.
✅ In simple words:
A PL/SQL program is written in blocks, and each block can contain variable declarations, executable 
statements, and error handling.

4. What is a PL/SQL cursor?
-----------------------------
A PL/SQL Cursor is used to store and process the result of a SQL query.
When a SQL statement returns rows from a table, a cursor helps PL/SQL read those rows one by one.
The group of rows returned by the SQL query is called the active set, and the cursor points to one row at a time.
PL/SQL provides two types of cursors:
1. Implicit Cursor
An Implicit Cursor is automatically created by Oracle when a SQL statement like INSERT, UPDATE, DELETE, 
or SELECT INTO is executed.
The programmer does not need to create it manually.
Example:
BEGIN
   UPDATE employees
   SET salary = salary + 1000
   WHERE department_id = 10;
   DBMS_OUTPUT.PUT_LINE(SQL%ROWCOUNT || ' rows updated');
END;
/
Explanation:
Oracle automatically creates an implicit cursor.
SQL%ROWCOUNT shows how many rows were updated.
2. Explicit Cursor
An Explicit Cursor is created by the programmer when a query returns multiple rows and we need to process them one by one.
Steps to use an explicit cursor:
Declare the cursor
Open the cursor
Fetch rows from the cursor
Close the cursor
Example:
DECLARE
   CURSOR emp_cursor IS
   SELECT name FROM employees;
   emp_name employees.name%TYPE;
BEGIN
   OPEN emp_cursor;
   LOOP
      FETCH emp_cursor INTO emp_name;
      EXIT WHEN emp_cursor%NOTFOUND;
      DBMS_OUTPUT.PUT_LINE(emp_name);
   END LOOP;
   CLOSE emp_cursor;
END;
/
Explanation:
Cursor emp_cursor selects employee names.
OPEN starts the cursor.

5. What is the use of WHERE CURRENT OF in cursors?
---------------------------------------------------
WHERE CURRENT OF is used with explicit cursors to update or delete the row that was last fetched by the cursor.
When a cursor fetches rows one by one, WHERE CURRENT OF helps identify the current row that the cursor is pointing to, 
so we can 
update or delete that row easily.
It is usually used with SELECT ... FOR UPDATE.
1. Syntax for Update
UPDATE table_name
SET column_name = value
WHERE CURRENT OF cursor_name;
2. Syntax for Delete
DELETE FROM table_name
WHERE CURRENT OF cursor_name;
FETCH retrieves one row at a time.
EXIT WHEN %NOTFOUND stops when no rows remain.
CLOSE releases the cursor.
✅ In simple words:
A cursor is a pointer used to handle the rows returned by a SQL query, especially when the query returns multiple rows.

6. How can a name be assigned to an unnamed PL/SQL Exception Block?
-------------------------------------------------------------------
In PL/SQL, some errors have error numbers but no predefined names.
To give a name to such errors, we use the PRAGMA EXCEPTION_INIT directive.
PRAGMA EXCEPTION_INIT connects a user-defined exception name with a specific Oracle error number.
After assigning the name, we can handle that error easily in the EXCEPTION block.
Syntax
DECLARE
   exception_name EXCEPTION;
   PRAGMA EXCEPTION_INIT(exception_name, error_code);
BEGIN
   -- code
EXCEPTION
   WHEN exception_name THEN
      -- exception handling code
END;
/
Example
Example: Handling the duplicate value error (ORA-00001).
DECLARE
   duplicate_error EXCEPTION;
   PRAGMA EXCEPTION_INIT(duplicate_error, -1);
BEGIN
   INSERT INTO employees(emp_id, emp_name, salary)
   VALUES (1, 'Rahul', 30000);
EXCEPTION
   WHEN duplicate_error THEN
      DBMS_OUTPUT.PUT_LINE('Duplicate value error occurred');
END;
/
Explanation
duplicate_error EXCEPTION;
Creates a user-defined exception name.
PRAGMA EXCEPTION_INIT(duplicate_error, -1);
Links the name with Oracle error number -1 (duplicate value).
If the error occurs, the EXCEPTION block handles it and prints the message.
✅ In simple words:
PRAGMA EXCEPTION_INIT is used to assign a name to an Oracle error number so that it can be handled easily in the EXCEPTION block.
7. What is a PL/SQL Trigger? Give some examples of when "Triggers" might be useful.
----------------------------------------------------------------------------------------
A PL/SQL Trigger is a stored program in the database that automatically executes when a specific event occurs on a table or view.
Events that can fire a trigger are:
INSERT (when a record is inserted)
UPDATE (when a record is updated)
DELETE (when a record is deleted)
A trigger runs automatically when the event happens. It cannot be executed manually.
Basic Syntax of Trigger
CREATE OR REPLACE TRIGGER trigger_name
BEFORE INSERT OR UPDATE OR DELETE
ON table_name
FOR EACH ROW
BEGIN
   -- trigger action
END;
/
Example 1: Trigger for Insert
This trigger prints a message when a new employee is inserted.
CREATE OR REPLACE TRIGGER emp_insert_trigger
BEFORE INSERT
ON employees
FOR EACH ROW
BEGIN
   DBMS_OUTPUT.PUT_LINE('New employee inserted');
END;
/
Test Query
INSERT INTO employees VALUES (6,'Ravi',42000,10);
Example 2: Trigger to Prevent Negative Salary
This trigger stops inserting a salary less than 0.
CREATE OR REPLACE TRIGGER salary_check_trigger
BEFORE INSERT OR UPDATE
ON employees
FOR EACH ROW
BEGIN
   IF :NEW.salary < 0 THEN
      RAISE_APPLICATION_ERROR(-20001,'Salary cannot be negative');
   END IF;
END;
/
Examples of When Triggers Are Useful
1. Maintaining Data Integrity
Triggers ensure that invalid data cannot be inserted or updated.
2. Enforcing Business Rules
For example, salary cannot be negative or age must be above 18.
3. Auditing Table Changes
Triggers can record who changed data and when.
4. Automatic Actions After Data Change
For example, when a new order is inserted, automatically update stock quantity.
5. Prevent Fraudulent Transactions
Triggers can block unauthorized updates or deletions.
✅ In simple words:
A Trigger is an automatic program in the database that runs when data is inserted, updated, or deleted from a table.
8. When does a DECLARE block have to be present?
----------------------------------------------------
In PL/SQL, the DECLARE block is used to define variables, constants, cursors, or exceptions before the program execution starts.
The DECLARE section is required only when we need to declare variables or other objects in the program.
If no variables or objects are needed, the DECLARE section can be omitted.
The DECLARE block is mainly used in anonymous PL/SQL blocks (stand-alone blocks that are not stored in the database).
Basic Structure
DECLARE
   -- variable declaration
BEGIN
   -- executable statements
END;
/
Example with DECLARE block
DECLARE
   num NUMBER := 10;
BEGIN
   DBMS_OUTPUT.PUT_LINE('Value of number: ' || num);
END;
/
Explanation
DECLARE → variable num is declared.
BEGIN → program logic executes.
DBMS_OUTPUT.PUT_LINE → prints the value.
Example without DECLARE block
If no variables are needed, we can write:
BEGIN
   DBMS_OUTPUT.PUT_LINE('Hello PL/SQL');
END;
/
✅ In simple words:
The DECLARE block is used when we need to define variables or other objects before executing the PL/SQL program. 
If nothing needs to be declared, the DECLARE section is not required.

9. How should comments be written in PL/SQL code?
-------------------------------------------------
Two types of comments we can write in PL/SQL code.
Single Line Comments
Multiple Line Comments
Single Line Comments: We can use the -- symbol to comment on a single line in PL/SQL code.
Multiple Line Comments: We can use the/* lines*/ syntax to comment on multiple lines in PL/SQL code.
DECLARE
-- Hi Geeks, This is a single line comment.
BEGIN
/* Hi Geeks,
This is Multiple line comments.*/
END;

10. What is the purpose of the WHEN condition in the trigger?
--------------------------------------------------------------
The WHEN condition is used in row-level triggers to specify when the trigger should execute.
The trigger will run only if the condition is TRUE.
Example
CREATE OR REPLACE TRIGGER salary_check
BEFORE UPDATE ON employees
FOR EACH ROW
WHEN (NEW.salary < 0)
BEGIN
   RAISE_APPLICATION_ERROR(-20001,'Salary cannot be negative');
END;
/
✅ The trigger runs only if salary becomes negative.
11. What are the Differences between SQL and PL/SQL?
----------------------------------------------------
SQL==>	PL/SQL
SQL is used to manage databases. ==>	PL/SQL is a programming language for Oracle databases.
Executes one statement at a time. ==>	Executes multiple statements in a block.
SQL is declarative language.	==> PL/SQL is procedural language.
No variables are used. ==>	Variables and data types are used.
Used for DDL and DML operations. ==>	Used to create procedures, functions, triggers, packages.
SQL cannot contain PL/SQL.	==>PL/SQL can contain SQL statements.
12. Why are SYSDATE and USER keywords used?
-------------------------------------------
SYSDATE
SYSDATE returns the current date and time of the database server.
SELECT SYSDATE FROM dual;
Example Output:
15-MAR-26 02:30:00
USER
USER returns the current logged-in database username.
SELECT USER FROM dual;
Example Output
HR
13. What is the Difference between implicit cursor and explicit cursor?
-------------------------------------------------------------------------
Implicit Cursor	Explicit Cursor
Automatically created by Oracle	Created by programmer
Used for single row queries	Used for multiple rows
Less control	Full control
Used with INSERT, UPDATE, DELETE, SELECT INTO	Used with SELECT returning multiple rows
Example implicit cursor attribute:
SQL%ROWCOUNT
SQL%FOUND
SQL%NOTFOUND
Example explicit cursor attribute:
cursor_name%ROWCOUNT
cursor_name%FOUND
cursor_name%NOTFOUND
14. Tell the importance of %TYPE and %ROWTYPE data types in PL/SQL.
-------------------------------------------------------------------
%TYPE
%TYPE is used to declare a variable with the same datatype as a table column.
Example
DECLARE
   v_salary employees.salary%TYPE;
BEGIN
   NULL;
END;
/
Here v_salary gets the same datatype as salary column.
%ROWTYPE
%ROWTYPE is used to store an entire row of a table.
Example
DECLARE
   emp_rec employees%ROWTYPE;
BEGIN
   SELECT * INTO emp_rec
   FROM employees
   WHERE emp_id = 1;
END;
/
15. What are the differences between ROLLBACK and ROLLBACK TO statements in PL/SQL?
-------------------------------------------------------------------------------------
ROLLBACK	ROLLBACK TO
Undo all changes in transaction	Undo changes up to a SAVEPOINT
Transaction ends	Transaction continues
Example
SAVEPOINT sp1;
UPDATE employees SET salary=50000 WHERE emp_id=1;
ROLLBACK TO sp1;
16. What are the uses of SYS.ALL_DEPENDENCIES?
-----------------------------------------------
SYS.ALL_DEPENDENCIES shows dependencies between database objects.
It displays relationships between:
Procedures
Packages
Triggers
Functions
Example
SELECT * FROM SYS.ALL_DEPENDENCIES;
17. What virtual tables exist during execution of a trigger?
-------------------------------------------------------------------
Two virtual tables exist in triggers:
OLD
Contains old values before update or delete.
NEW
Contains new values after insert or update.
Operation	OLD	NEW
INSERT	❌	✔
UPDATE	✔	✔
DELETE	✔	❌
Example
:OLD.salary
:NEW.salary
18. Difference between cursors declared in procedures and in package specifications?
Procedure Cursor	Package Cursor
Local to procedure	Global
Cannot be used outside procedure	Can be used by all procedures in package
19. What is purpose of COMMIT, ROLLBACK and SAVEPOINT?
-----------------------------------------------------------
COMMIT
Saves changes permanently in database.
COMMIT;
ROLLBACK
Undo changes since last commit.
ROLLBACK;
SAVEPOINT
Creates a checkpoint in transaction.
SAVEPOINT sp1;
ROLLBACK TO sp1;
20. How can we debug our PL/SQL code?
--------------------------------------
PL/SQL debugging can be done using:
1️⃣ DBMS_OUTPUT
DBMS_OUTPUT.PUT_LINE('Debug message');
Prints output in console.
2️⃣ DBMS_DEBUG
Used for advanced debugging and logging.
✅ Simple answer:
To debug PL/SQL code we use DBMS_OUTPUT to print messages and DBMS_DEBUG for detailed debugging.

21. What is the main difference between a mutating table and a constraining table?
------------------------------------------------------------------------------------
Mutating Table
A mutating table is a table that is currently being modified by a DML operation like:
INSERT
UPDATE
DELETE
When a trigger tries to read or modify the same table during this operation, Oracle raises a mutating table error.
Example:
If a trigger on employees tries to query the same employees table while updating it, the table becomes a mutating table.
Constraining Table
A constraining table is a table that is referenced by a foreign key constraint to maintain referential integrity.
Example
Departments  ← Parent Table
Employees    ← Child Table
Here Departments is a constraining table.
✅ Simple Difference
Mutating Table	Constraining Table
Table currently being modified	Table used for referential integrity
Used in triggers	Used in foreign key constraints
22. Describe the data types present in PL/SQL.
----------------------------------------------
PL/SQL mainly has two types of data types.
1. Scalar Data Types
These store single values.
Examples:
NUMBER
DATE
CHAR
VARCHAR2
BOOLEAN
LONG
Example
DECLARE
   v_name VARCHAR2(50);
   v_salary NUMBER;
BEGIN
   v_name := 'Rahul';
   v_salary := 30000;
END;
/
2. Composite Data Types
These store multiple values together.
Examples:
RECORD
TABLE (collections)
Example
DECLARE
   TYPE emp_record IS RECORD(
      emp_id NUMBER,
      emp_name VARCHAR2(50)
   );
   emp emp_record;
BEGIN
   emp.emp_id := 1;
   emp.emp_name := 'Rahul';
END;
/
23. List the types of exceptions in PL/SQL.
----------------------------------------------
There are two types of exceptions in PL/SQL.
1. Predefined Exceptions
These are already defined by Oracle.
Examples:
NO_DATA_FOUND
TOO_MANY_ROWS
ZERO_DIVIDE
Example
BEGIN
   SELECT salary INTO v_salary
   FROM employees
   WHERE emp_id = 100;
EXCEPTION
   WHEN NO_DATA_FOUND THEN
      DBMS_OUTPUT.PUT_LINE('No record found');
END;
/
2. User-defined Exceptions
These are created by programmers to handle custom errors.
Example
DECLARE
   salary_error EXCEPTION;
BEGIN
   IF salary < 0 THEN
      RAISE salary_error;
   END IF;
END;
/
24. What types of commands PL/SQL does not support?
----------------------------------------------------
PL/SQL does not directly support DDL commands such as:
CREATE
ALTER
DROP
TRUNCATE
These commands must be executed using dynamic SQL.
Example
EXECUTE IMMEDIATE 'CREATE TABLE test(id NUMBER)';
25. Name some PL/SQL exceptions.
----------------------------------
Some common PL/SQL predefined exceptions are:
INVALID_NUMBER – Invalid number conversion
TOO_MANY_ROWS – Query returns more than one row
ACCESS_INTO_NULL – Attempt to assign value to null object
CASE_NOT_FOUND – CASE statement without matching condition
ZERO_DIVIDE – Division by zero
NO_DATA_FOUND – Query returns no rows
Example
DECLARE
   num NUMBER;
BEGIN
   num := 10/0;
EXCEPTION
   WHEN ZERO_DIVIDE THEN
      DBMS_OUTPUT.PUT_LINE('Cannot divide by zero');
END;
/

26. What is a PL/SQL package?
------------------------------
A PL/SQL Package is a database object that groups related procedures, functions, variables, and cursors together.
It helps organize large PL/SQL programs and makes code reusable and easier to maintain.
A package has two parts.
1. Package Specification
Contains declarations of procedures, functions, variables, and cursors.
It acts like an interface.
2. Package Body
Contains the actual implementation of procedures and functions declared in the specification.
Example
Package Specification
CREATE OR REPLACE PACKAGE emp_pkg AS
   PROCEDURE show_message;
END emp_pkg;
/
Package Body
CREATE OR REPLACE PACKAGE BODY emp_pkg AS
   PROCEDURE show_message IS
   BEGIN
      DBMS_OUTPUT.PUT_LINE('Hello from package');
   END;
END emp_pkg;
/
27. Write a PL/SQL program to find a given string is palindrome.
-----------------------------------------------------------------
A palindrome is a string that reads the same forward and backward.
Example:
madam, level, radar
PL/SQL Program
DECLARE
   str VARCHAR2(20) := 'madam';
   reverse_string VARCHAR2(20) := '';
BEGIN
   FOR i IN REVERSE 1..LENGTH(str) LOOP
      reverse_string := reverse_string || SUBSTR(str,i,1);
   END LOOP;
   IF str = reverse_string THEN
      DBMS_OUTPUT.PUT_LINE(str || ' is palindrome');
   ELSE
      DBMS_OUTPUT.PUT_LINE(str || ' is not palindrome');
   END IF;
END;
/
Output
madam is palindrome
28. What command will you use to delete a package?
--------------------------------------------------
We use the DROP PACKAGE command to delete a package.
Syntax
DROP PACKAGE package_name;
Example
DROP PACKAGE emp_pkg;
If you want to delete only the package body, use:
DROP PACKAGE BODY emp_pkg;
29. How will you execute a stored procedure?
-----------------------------------------------
A stored procedure can be executed using EXECUTE or EXEC command.
Syntax
EXEC procedure_name;
or
EXECUTE procedure_name;
Example
EXEC show_message;
Another method:
BEGIN
   show_message;
END;
/
30. Explain the IN, OUT and IN OUT parameters.
----------------------------------------------
PL/SQL procedures use three types of parameters.
1. IN Parameter
Used to pass values to the procedure
Default parameter type
Acts like a constant (cannot be modified)
Example
PROCEDURE show_name(name IN VARCHAR2)
2. OUT Parameter
Used to return values from the procedure
Value is assigned inside the procedure
Example
PROCEDURE get_salary(salary OUT NUMBER)
3. IN OUT Parameter
Used to pass a value to the procedure and return the updated value
Example
PROCEDURE update_salary(salary IN OUT NUMBER)
Simple Example
CREATE OR REPLACE PROCEDURE add_num(
   a IN NUMBER,
   b IN NUMBER,
   result OUT NUMBER
)
IS
BEGIN
   result := a + b;
END;
/
Call the procedure
DECLARE
   res NUMBER;
BEGIN
   add_num(10,20,res);
   DBMS_OUTPUT.PUT_LINE(res);
END;
/
Output
30


41. How can we write or create multiple tables in PL/SQL?
-------------------------------------------------------------
In PL/SQL, Nested Tables are used to store multiple values.
Nested tables are a type of collection similar to a dynamic array.
They can store multiple elements of the same datatype, and their size can increase or decrease dynamically.
Syntax
TYPE type_name IS TABLE OF element_type;
table_name type_name;
Example
DECLARE
   TYPE number_table IS TABLE OF NUMBER;
   numbers number_table := number_table(10,20,30);
BEGIN
   FOR i IN 1..numbers.COUNT LOOP
      DBMS_OUTPUT.PUT_LINE(numbers(i));
   END LOOP;
END;
/
42. Discuss the concept of RAISE_APPLICATION_ERROR.
----------------------------------------------------
RAISE_APPLICATION_ERROR is used to generate user-defined error messages in PL/SQL.
It helps the programmer stop program execution and display a custom error message.
Syntax
RAISE_APPLICATION_ERROR(error_number, message);
Error numbers must be between -20000 and -20999.
Example
BEGIN
   IF 100 < 0 THEN
      RAISE_APPLICATION_ERROR(-20001,'Invalid salary');
   END IF;
END;
/
43. What do you know about PRAGMA EXCEPTION_INIT in PL/SQL?
--------------------------------------------------------
PRAGMA EXCEPTION_INIT is used to associate a user-defined exception with a specific Oracle error number.
This allows the programmer to handle that error using a custom exception name.
Syntax
DECLARE
   exception_name EXCEPTION;
   PRAGMA EXCEPTION_INIT(exception_name, error_number);
Example
DECLARE
   dup_error EXCEPTION;
   PRAGMA EXCEPTION_INIT(dup_error,-1);
BEGIN
   NULL;
EXCEPTION
   WHEN dup_error THEN
      DBMS_OUTPUT.PUT_LINE('Duplicate value error');
END;
/
44. How can you verify whether an UPDATE statement is executed or not in PL/SQL?
--------------------------------------------------------------------------------
We use the SQL%NOTFOUND attribute.
If the UPDATE statement does not affect any rows, SQL%NOTFOUND returns TRUE.
Example
BEGIN
   UPDATE employees
   SET salary = 40000
   WHERE emp_id = 10;
   IF SQL%NOTFOUND THEN
      DBMS_OUTPUT.PUT_LINE('No rows updated');
   END IF;
END;
/
45. What is the use of the || operator?
----------------------------------------
The || operator is used to concatenate (combine) strings.
Example
BEGIN
   DBMS_OUTPUT.PUT_LINE('Hello ' || 'World');
END;
/
Output
Hello World
46. Is a definition command like CREATE supported in PL/SQL?
---------------------------------------------------------------
PL/SQL does not directly support DDL commands like:
CREATE
ALTER
DROP
But these commands can be executed using dynamic SQL.
Example
EXECUTE IMMEDIATE 'CREATE TABLE test(id NUMBER)';
47. Explain a view.
-------------------
A View is a virtual table created from one or more tables using a SQL query.
It does not store data itself but displays data from base tables.
Syntax
CREATE VIEW view_name AS
SELECT column1, column2
FROM table_name;
Example
CREATE VIEW emp_view AS
SELECT emp_id, emp_name
FROM employees;
48. What are the basic parts of triggers?
-----------------------------------------
A trigger has three basic parts.
1. Trigger Statement
Defines when the trigger should fire.
Example
BEFORE INSERT ON employees
2. Trigger Restriction
Defines conditions using WHEN clause.
Example
WHEN (NEW.salary > 10000)
3. Trigger Action
Contains the PL/SQL code that executes.
Example
BEGIN
   DBMS_OUTPUT.PUT_LINE('Trigger executed');
END;
49. What are the methods to trace PL/SQL code?
-------------------------------------------------
Tracing helps monitor program execution and performance.
Common tracing tools:
DBMS_TRACE – Traces program execution
DBMS_APPLICATION_INFO – Stores application information
DBMS_SESSION – Controls session settings
DBMS_MONITOR – Monitors database performance
50. How do you create nested tables in PL/SQL?
-----------------------------------------------
Nested tables are collections used to store multiple values.
Syntax
TYPE type_name IS TABLE OF element_type;
Example
DECLARE
   TYPE dept_table IS TABLE OF VARCHAR2(20);
   departments dept_table := dept_table('Marketing','Development','Sales');
BEGIN
   FOR i IN 1..departments.COUNT LOOP
      DBMS_OUTPUT.PUT_LINE('Department: ' || departments(i));
   END LOOP;
END;
/
Output
Department: Marketing
Department: Development
Department: Sales
